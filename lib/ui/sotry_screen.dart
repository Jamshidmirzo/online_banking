import 'package:flutter/material.dart';

class SotryScreen extends StatelessWidget {
  const SotryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.amber.shade100,
        title: Text("Story Screen"),
      ),
    );
  }
}
